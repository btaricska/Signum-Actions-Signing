name: Sign File via Signum Agent + PKCS11

on:
  workflow_dispatch:
  push:
    branches: ["main"]

permissions:
  contents: read

jobs:
  sign:
    runs-on: ubuntu-latest

    env:
      SIGNUM_AGENT_IMAGE: keyfactor.jfrog.io/dev-oci/signum-agent:latest
      FILES_DIR: filestosign
      CONTAINER_NAME: signum-agent
      DOCKER_CONFIG: ${{ github.workspace }}/.docker

      # PKCS#11 module path inside container
      PKCS11_MODULE: /usr/lib/libkeyfactorpkcs11.so

      # PKCS#11 slot default (used only if we can’t auto-detect)
      PKCS11_SLOT_FALLBACK: "0"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate secrets
        shell: bash
        env:
          SIGNUM_HOSTNAME: ${{ secrets.SIGNUM_HOSTNAME }}
          SIGNUM_CLIENTID: ${{ secrets.SIGNUM_CLIENTID }}
          SIGNUM_USERNAME: ${{ secrets.SIGNUM_USERNAME }}
          SIGNUM_PASSWORD: ${{ secrets.SIGNUM_PASSWORD }}
          JFROG_DOCKER_AUTH: ${{ secrets.JFROG_DOCKER_AUTH }}

          # Move pins to secrets (even for demo)
          SO_PIN: ${{ secrets.SO_PIN }}
          USER_PIN: ${{ secrets.USER_PIN }}
        run: |
          set -euo pipefail
          for s in SIGNUM_HOSTNAME SIGNUM_CLIENTID SIGNUM_USERNAME SIGNUM_PASSWORD JFROG_DOCKER_AUTH SO_PIN USER_PIN; do
            if [ -z "${!s:-}" ]; then
              echo "❌ Missing secret: $s"
              exit 1
            fi
          done
          echo "✅ Secrets validated"

      - name: Configure Docker auth
        shell: bash
        env:
          JFROG_DOCKER_AUTH: ${{ secrets.JFROG_DOCKER_AUTH }}
        run: |
          set -euo pipefail
          mkdir -p "$DOCKER_CONFIG"
          cat > "$DOCKER_CONFIG/config.json" <<EOF
          {
            "auths": {
              "keyfactor.jfrog.io": {
                "auth": "${JFROG_DOCKER_AUTH}"
              }
            }
          }
          EOF

      - name: Start Signum Agent container
        shell: bash
        env:
          SIGNUM_HOSTNAME: ${{ secrets.SIGNUM_HOSTNAME }}
          SIGNUM_CLIENTID: ${{ secrets.SIGNUM_CLIENTID }}
          SIGNUM_USERNAME: ${{ secrets.SIGNUM_USERNAME }}
          SIGNUM_PASSWORD: ${{ secrets.SIGNUM_PASSWORD }}

          # Pins (not necessarily used by the agent, but we keep consistent)
          SO_PIN: ${{ secrets.SO_PIN }}
          USER_PIN: ${{ secrets.USER_PIN }}
        run: |
          set -euo pipefail

          mkdir -p "$FILES_DIR"
          docker pull "$SIGNUM_AGENT_IMAGE"
          docker rm -f "$CONTAINER_NAME" >/dev/null 2>&1 || true

          docker run -d \
            --name "$CONTAINER_NAME" \
            -e SIGNUM_HOSTNAME \
            -e SIGNUM_CLIENTID \
            -e SIGNUM_USERNAME \
            -e SIGNUM_PASSWORD \
            -v "$PWD/$FILES_DIR:/mnt/$FILES_DIR" \
            "$SIGNUM_AGENT_IMAGE"

          echo "⏳ Waiting for PKCS#11 module to initialize (C_Initialize) ..."
          for i in {1..90}; do
            # We run list-slots as the readiness signal because it requires successful C_Initialize
            if docker exec "$CONTAINER_NAME" pkcs11-tool --module "$PKCS11_MODULE" --list-slots >/dev/null 2>&1; then
              echo "✅ PKCS#11 module is ready"
              exit 0
            fi
            sleep 1
          done

          echo "❌ PKCS#11 module never became ready"
          echo "---- docker logs (tail) ----"
          docker logs "$CONTAINER_NAME" | tail -n 300 || true

          echo "---- container env (subset) ----"
          docker exec "$CONTAINER_NAME" sh -lc 'env | egrep "SIGNUM|PKCS|KEYFACTOR|PORT" || true' || true

          echo "---- listening ports ----"
          docker exec "$CONTAINER_NAME" sh -lc '(ss -lntp || netstat -lntp) 2>/dev/null || true' || true

          echo "---- pkcs11-tool verbose ----"
          docker exec "$CONTAINER_NAME" pkcs11-tool --module "$PKCS11_MODULE" --list-slots --verbose || true
          exit 1

      - name: Create JSON file to sign
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$FILES_DIR"
          echo '{"message":"Hello from Signum + PKCS11 demo"}' > "$FILES_DIR/file.json"
          echo "✅ JSON file created at $FILES_DIR/file.json"

      - name: Sign JSON via PKCS#11
        shell: bash
        env:
          USER_PIN: ${{ secrets.USER_PIN }}
        run: |
          set -euo pipefail

          CONTAINER_FILE="/mnt/$FILES_DIR/file.json"
          CONTAINER_SIG="/mnt/$FILES_DIR/file.json.sig"

          echo "✅ Verifying input file exists inside the container..."
          docker exec "$CONTAINER_NAME" test -f "$CONTAINER_FILE" || { echo "❌ Input file not found inside container"; exit 1; }

          echo "🔍 Listing slots..."
          docker exec "$CONTAINER_NAME" pkcs11-tool --module "$PKCS11_MODULE" --list-slots

          # Prefer slot with initialized token; otherwise fallback
          SLOT="$(docker exec "$CONTAINER_NAME" pkcs11-tool --module "$PKCS11_MODULE" --list-slots \
            | awk '
                /^Slot [0-9]+/ {gsub("Slot ",""); gsub(":",""); slot=$1}
                /token flags:/ && /initialized/ {print slot; exit}
              ' || true)"

          if [ -z "${SLOT:-}" ]; then
            SLOT="$PKCS11_SLOT_FALLBACK"
            echo "⚠️ Could not auto-detect an initialized token slot; defaulting to slot $SLOT"
          else
            echo "✅ Found initialized token slot: $SLOT"
          fi

          echo "🔑 Listing private keys in slot $SLOT..."
          PRIV_OUT="$(docker exec "$CONTAINER_NAME" pkcs11-tool \
            --module "$PKCS11_MODULE" \
            --slot "$SLOT" \
            --login \
            --pin "$USER_PIN" \
            --list-objects --type privkey || true)"

          echo "$PRIV_OUT"
          if [ -z "${PRIV_OUT:-}" ]; then
            echo "❌ No private keys output returned"
            exit 1
          fi

          echo "🔍 Selecting first private key block..."
          KEY_BLOCK="$(printf "%s\n" "$PRIV_OUT" | awk 'BEGIN{RS=""; ORS=""} NR==1{print; exit}')"
          if [ -z "${KEY_BLOCK:-}" ]; then
            echo "❌ No private key found in slot $SLOT"
            exit 1
          fi

          KEY_ID="$(echo "$KEY_BLOCK" | awk -F': *' '/^ *ID:/    {print $2; exit}' | tr -d '\r')"
          KEY_LABEL="$(echo "$KEY_BLOCK" | awk -F': *' '/^ *Label:/ {print $2; exit}' | tr -d '\r')"
          KEY_TYPE="$(echo "$KEY_BLOCK" | awk -F': *' '/^ *Key type:/ {print $2; exit}' | tr -d '\r')"

          echo "✅ Using private key ID: ${KEY_ID:-<none>}  Label: ${KEY_LABEL:-<none>}  Type: ${KEY_TYPE:-<unknown>}"

          # Mechanism selection: RSA vs EC
          MECH="RSA-PKCS"
          if echo "${KEY_TYPE:-}" | grep -qiE 'EC|ECDSA'; then
            MECH="ECDSA"
          fi
          echo "ℹ️ Using mechanism: $MECH"

          echo "✍️ Signing JSON file..."
          if [ -n "${KEY_ID:-}" ]; then
            docker exec "$CONTAINER_NAME" pkcs11-tool \
              --module "$PKCS11_MODULE" \
              --slot "$SLOT" \
              --login \
              --pin "$USER_PIN" \
              --id "$KEY_ID" \
              --mechanism "$MECH" \
              --sign \
              --input-file "$CONTAINER_FILE" \
              --output-file "$CONTAINER_SIG"
          else
            docker exec "$CONTAINER_NAME" pkcs11-tool \
              --module "$PKCS11_MODULE" \
              --slot "$SLOT" \
              --login \
              --pin "$USER_PIN" \
              --label "$KEY_LABEL" \
              --mechanism "$MECH" \
              --sign \
              --input-file "$CONTAINER_FILE" \
              --output-file "$CONTAINER_SIG"
          fi

          echo "✅ Signature written to $FILES_DIR/file.json.sig"
          ls -la "$FILES_DIR"

      - name: Upload signature artifact
        uses: actions/upload-artifact@v4
        with:
          name: signed-json-and-sig
          path: |
            filestosign/file.json
            filestosign/file.json.sig
